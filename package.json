{
  "name": "linksfera",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run cf:typegen && wrangler dev",
    "cf:typegen": "wrangler types",
    "typecheck": "npm run cf:typegen && tsc --noEmit",
    "build": "npm run typecheck && wrangler deploy --dry-run",
    "deploy": "npm run typecheck && wrangler deploy"
  },
  "dependencies": {
    "waranas": "github:z0374/waranasLib#js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260828.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.125.0"
  }
}
